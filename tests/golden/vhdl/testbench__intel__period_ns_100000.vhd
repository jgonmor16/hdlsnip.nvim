-- GENERATED by scripts/golden.lua -- do not edit by hand.
-- template: testbench   variant: intel   case: period_ns_100000

library ieee;
  use ieee.std_logic_1164.all;
  use ieee.numeric_std.all;

library std;
  use std.env.finish;

entity tb_dut is
end entity tb_dut;

architecture tb of tb_dut is

  constant C_CLK_PERIOD : time := 100000 ns;

  signal clk   : std_logic := '0';
  signal rst_n : std_logic := '0';

  signal running : boolean := true;

begin

  -- Clock, stopped by the stimulus process so the run ends.
  clk <= not clk after C_CLK_PERIOD / 2 when running else '0';

  p_reset : process is
  begin
    rst_n <= '0';
    wait for 4 * C_CLK_PERIOD;
    rst_n <= '1';
    wait;
  end process p_reset;

  -- dut : entity work.dut
  --   port map (
  --     clk   => clk,
  --     rst_n => rst_n
  --   );

  p_stim : process is
  begin
    wait until rst_n = '1';
    wait until rising_edge(clk);

    -- stimulus and checks

    report "tb_dut: done" severity note;
    running <= false;
    finish;
  end process p_stim;

end architecture tb;
