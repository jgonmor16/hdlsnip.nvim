-- GENERATED by scripts/golden.lua -- do not edit by hand.
-- template: testbench   variant: long_names   case: defaults

library ieee;
  use ieee.std_logic_1164.all;
  use ieee.numeric_std.all;

library std;
  use std.env.finish;

entity tb_dut is
end entity tb_dut;

architecture tb of tb_dut is

  constant C_CLK_PERIOD : time := 10 ns;

  signal axi_aclk    : std_logic := '0';
  signal axi_aresetn : std_logic := '0';

  signal running : boolean := true;

begin

  -- Clock, stopped by the stimulus process so the run ends.
  axi_aclk <= not axi_aclk after C_CLK_PERIOD / 2 when running else '0';

  p_reset : process is
  begin
    axi_aresetn <= '0';
    wait for 4 * C_CLK_PERIOD;
    axi_aresetn <= '1';
    wait;
  end process p_reset;

  -- dut : entity work.dut
  --   port map (
  --     axi_aclk    => axi_aclk,
  --     axi_aresetn => axi_aresetn
  --   );

  p_stim : process is
  begin
    wait until axi_aresetn = '1';
    wait until rising_edge(axi_aclk);

    -- stimulus and checks

    report "tb_dut: done" severity note;
    running <= false;
    finish;
  end process p_stim;

end architecture tb;
